using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class TrainCardScript : MonoBehaviour
{
    public string color;

    // Start is called before the first frame update
    void Start()
    {
        //Debug.Log("TrainCardScript");
        //if (transform.GetChild(1).GetComponent<Image>())
        //{
        //    color = transform.GetChild(1).GetComponent<Image>().sprite.name.Substring(9);
        //}
        //if (transform.GetChild(1).GetComponent<SpriteRenderer>())
        //{
        //    color = transform.GetChild(1).GetComponent<SpriteRenderer>().sprite.name.Substring(9);
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
